const bf = require("./interpreter.js");

// js

// Parsing a boilerplate
console.log(bf.parse(bf.boilerplate.helloWorld));

// The whole output of the parser
console.log(bf.parse(bf.boilerplate.helloWorld));
